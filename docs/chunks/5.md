# Chunk 5 — Clarity view + completion digests

You are implementing Chunk 5 of Galapagos. Read `docs/vision.md` and
`docs/architecture.md` first — they are the binding contract and override this
brief on any conflict.

## State when you start

Chunks 1–4 done: chat, records, workers with parsed completion digests,
monitoring, evidence, confidence gauges, attention queue, triage.

## Build

- Clarity view-model (`core/clarity/view-model.ts`): rewrite trimmed from the
  prior prototype's pattern
  (`/Users/taan/Dev/karz98rk/src/manager-clarity/view-model.ts`; test patterns
  in `.../tests/manager-clarity-view-model.test.ts`). Non-negotiable pattern:
  **every displayed field carries source attribution** (source, sourceLabel,
  sourceRecords) and missing data renders as explicitly missing with source
  "missing" — never fabricated, never defaulted.
- Per-worker clarity: **now** (last_summary + live `git diff --numstat` vs
  `base_sha` + latest evidence) vs **inception** (worker_brief record + lane
  contract).
- Project clarity: latest synthesis + active goal vs aggregate worker state and
  open attention.
- `/clarity` page rendering both, calm-mission-control quiet.
- Completion digest UI as the lead surface of a finished worker's drilldown,
  in the four vision layers, in order:
  1. tiny narrative (≤3 sentences),
  2. before/after in product terms,
  3. visual change map — touched areas sized by change magnitude (numstat),
     colored in-lane / tested / untested / out-of-lane,
  4. claim checklist with verified/unverified/contradicted evidence badges,
     proof one click away (evidence run, diff, file).
  Raw transcript and full diffs exist only behind explicit drilldowns.
- `core/digests/assemble.ts`: pure assembly of digest layers from
  completion_digests + numstat + lane + evidence rows. Unit-tested.

## Forbidden

No fabricated or default-filled fields; no raw agent prose rendered by default
anywhere; no new tables or record types; no confidence semantics changes.

## Exit criterion

Every displayed field on `/clarity` is traceable to a named record or live
signal; a finished worker's result is absorbable from the digest alone —
narrative, before/after, change map, badges — without reading transcript text.

## Verification

- `npm test` green: view-model unit tests (source attribution, missing-data
  rendering), digest layer assembly tests.
- Manual: cross-check three displayed fields against their cited sources.
- Manual: run a real worker to completion and absorb the result from the
  digest only.
