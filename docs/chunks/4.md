# Chunk 4 — Monitoring, evidence, confidence

You are implementing Chunk 4 of Galapagos. Read `docs/vision.md` and
`docs/architecture.md` first — they are the binding contract and override this
brief on any conflict. Consult the Claude Agent SDK docs — do not guess SDK
APIs.

## State when you start

Chunks 1–3 done: manager chat, records + distillation, workers streaming in
lanes with completion digests parsed and attention_items existing (no queue UI
yet, no monitor loop).

## Build

- Monitor loop (`src/daemon/monitor.ts`, default 30s tick, **zero LLM calls**):
  staleness (`last_message_at` beyond threshold while running), detective lane
  audit (diff vs `base_sha` ∪ porcelain → lane-check → `lane_violation` +
  `contradicted` evidence signal), evidence freshness (`evidence_runs.head_sha`
  vs worktree HEAD → stale), completion-claims scan (done-claims without
  passing evidence → `unsupported_claim`).
- Check runner: port mechanics from
  `/Users/taan/Dev/karz98rk/src/local-session-reporter/checks.ts` into
  `adapters/checks/run-checks.ts`; results into `evidence_runs` keyed to
  `head_sha`; `run_checks` manager tool.
- Confidence engine: port
  `/Users/taan/Dev/karz98rk/src/manager-clarity/confidence-engine.ts` into
  `core/confidence/engine.ts` near-verbatim (rename thread_brief→worker_brief,
  drop lifecycle/manager_run signal blocks, keep every cap and evidence
  semantic) plus its regression suites
  (`.../tests/manager-confidence-engine.test.ts`,
  `.../tests/confidence-engine.test.ts`) — semantics must stay identical.
- New thin evidence adapter: SQLite rows (evidence_runs, completion_digest
  claims, lane audits, worker liveness) → engine evidence signals. Link each
  digest claim to its evidence run so claims resolve to
  verified/unverified/contradicted.
- Attention queue UI: the one loud surface (vision) — open items ranked by
  priority, resolve/dismiss actions wired to SQLite.
- Event-driven manager triage (jobs kind `triage`): runs only when new open
  attention items exist since last triage, batched, small-model option. It can
  steer/stop workers, `ask_user`, and `resolve_attention`. **Management by
  exception:** clean completions are auto-reviewed
  (`completion_digests.status = manager_reviewed`); only failures,
  contradictions, and direction calls escalate.
- Per-worker and project confidence gauges rendered on `/` and `/workers`
  (one bar each; caps/signals in a debug drilldown only).

## Forbidden

No per-tick LLM calls, no new record types, no default sub-bars for confidence
dimensions, no confidence input from how confident agent prose sounds.

## Exit criterion

Gauges move for real reasons: a stale worker drains confidence, a failing
required check blocks it, and a worker's question reaches the user in chat via
triage — while a clean completion passes without interrupting the user.

## Verification

- `npm test` green: ported engine regression suite (identical semantics),
  evidence-adapter unit tests (row → signal mapping).
- Manual: kill a worker mid-task → stale → attention → triage escalation.
- Manual: complete a clean scoped task → digest `manager_reviewed`, no user
  interruption.
