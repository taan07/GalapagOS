# Chunk 2 — Durable memory + distillation

You are implementing Chunk 2 of Galapagos. Read `docs/vision.md` and
`docs/architecture.md` first — they are the binding contract and override this
brief on any conflict. Consult the Claude Agent SDK docs — do not guess SDK
APIs.

## State when you start

Chunk 1 is done: daemon on :4517, SQLite with
projects/manager_sessions/manager_turns/jobs, manager chat working end-to-end
with `git_truth`, `read_records` stubbed, resume proven.

## Build

- `core/records/frontmatter.ts`: port verbatim from
  `/Users/taan/Dev/karz98rk/src/obsidian/frontmatter.ts` (+ frontmatter test
  cases from `.../tests/obsidian.test.ts`).
- `core/records/schema.ts`: the 8 `glp_type` record types + validation rules
  (architecture §4). Decision records validate `decision_options`,
  `rollback_note`, `confidence_impact`; `chosen_path` required before closed
  status. (Checkpoint git-tagging itself is Chunk 6 — validation only here.)
- `adapters/records/store.ts`: port mechanics from
  `/Users/taan/Dev/karz98rk/src/server/manager-docs.ts` — read/index, create
  (wx flag, per-type subdirectories `docs/galapagos/<type-dir>/`,
  `YYYY-MM-DD-<slug>-<shortid>.md`), update (status transitions, append notes).
  Adapt that file's create/update validation tests.
- Wire real `read_records` / `write_record` / `update_record` manager tools.
- Distillation: post-turn `distill` job (jobs table) — one cheap follow-up
  prompt on the same session per architecture §5; mark covered turns
  `distilled_at`; auto-commit `docs/galapagos/` when files were written
  (commit message prefix `galapagos(records):`).
- Compact-by-re-brief: when resume fails or context is bloated, open a fresh
  session seeded from latest synthesis + active goal + open questions + open
  attention; mark old session `compacted`.
- `/records` page: plain browser of records (type, title, status, date, body),
  every field source-attributed. No editing UI.

## Forbidden

No git tags/checkpoints, no workers, no monitoring, no new SQLite tables, no
record types beyond the 8, no transcript dumps into records (doctrine only).

## Exit criterion

User tells the manager the project goal in chat → a committed
`docs/galapagos/goals/*.md` `active_goal` record exists; force a fresh session
(delete sdk session id or simulate resume failure) → the manager recalls the
goal via re-brief.

## Verification

- `npm test` green: record round-trip, validation, frontmatter suites.
- `git -C <target> log --oneline -- docs/galapagos` shows distill commits.
- Manual: goal drill above; `/records` shows the record with correct
  frontmatter.
